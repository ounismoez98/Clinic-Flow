spring:
  application:
    name: MSOrdonnance
  cloud:
    config:
      enabled: true
  config:
    import: "optional:configserver:http://localhost:8888"
management:
  endpoints:
    web:
      exposure:
        include: health,refresh
